apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: codesys-arm32-data-pvc
  namespace: codesys
  labels:
    app: codesys
    arch: arm32
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path  # k3s default storage class
